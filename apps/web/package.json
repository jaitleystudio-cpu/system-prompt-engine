{
  "name": "spe-web",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "SPE-WEB-01 System Prompt Engine product website foundation. Real WASM path. COST ₹0. not_a_release=true.",
  "license": "MIT OR Apache-2.0",
  "scripts": {
    "copy-wasm": "node scripts/copy-wasm.mjs",
    "dev": "npm run copy-wasm && vite",
    "build": "npm run copy-wasm && tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "test:engine": "node scripts/eval-fixture.mjs",
    "audit:deps": "node scripts/audit-deps.mjs",
    "audit:assets": "node scripts/measure-assets.mjs",
    "audit:egress": "node scripts/egress-proof.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
