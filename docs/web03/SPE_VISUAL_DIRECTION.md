# SPE WEB-03 — Semantic press

## Composition
An editorial instrument: porcelain typography on ink, warm titanium surfaces with fine mint light seams. A large architectural stack inhabits the right two-thirds, with a legible left headline and a full-width command shelf below. No diamond, orb, orbit, capability-card dashboard or decorative verification labels.

Headline: Give your thought structure. Product explanation: Turn a request into a structured instruction for any AI.

## Spatial system
Original curved metal leaves constructed procedurally in Three.js; layered in depth, illuminated by a generated studio environment. A controlled camera and pointer response reveal thickness and surfaces. On real successful compile, leaves align into an artifact. Geometry counts and labels derive only from the returned envelope; unknowns retain an open offset. Idle geometry is explicitly an illustration, not inferred user semantics.

## Data truth
Baseline free text is a goal atom; default intent contains supplied assumptions/questions. Do not claim automatic extraction, intelligence scores or semantic phases unsupported by Worker events. Show actual engine phase labels. Do not change Rust or WASM host, Worker, runtime semantics or frozen study data.

## Narrative
Thought → structure → portable artifact → private creation. Large sticky diagram with scroll-driven alignment; ordinary document scroll, no scroll capture. Live output appears next to its actual returned semantic breakdown. Workspace preserves Simple/Inspect/Pro, Intent and Prompt lenses and exports.

## Accessibility and performance
HTML is canonical, canvas decorative. Keyboard forms and explicit focus. Static CSS dimensional fallback for reduced motion/WebGL loss. Manual motion toggle. Canvas pauses offscreen/hidden. Lazy 3D chunk. No remote assets or runtime requests beyond same-origin app and WASM. No analytics.

## Acceptance
Three documented visual rounds, viewport matrix, real compile/offline/integrity checks, accessibility audit and performance measurements. No award claim. If any required gate is unresolved, final verdict REBUILD_REQUIRED.
