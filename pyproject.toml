[project]
name = "system-prompt-engine"
version = "0.0.0"
description = "System Prompt Engine (SPE) — NEW_IMPLEMENTATION from S0"
requires-python = ">=3.11"
readme = "README.md"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["spe_runtime*"]
